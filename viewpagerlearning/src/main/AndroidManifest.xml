<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wuzch.viewpagerlearning">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Guide"/>

        <activity android:name=".MainActivity"
            android:launchMode="singleTask"/>

        <activity android:name=".OneActivity"
            android:label="@string/one_activity_name"/>
        <activity android:name=".TwoActivity"
            android:label="@string/two_activity_name"/>
        <activity android:name=".ThreeActivity"
            android:label="@string/three_activity_name"/>
        <activity android:name=".FourActivity"
            android:label="@string/four_activity_name"/>
    </application>

</manifest>